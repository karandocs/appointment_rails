= simple_form_for(Appointment.new, remote: true) do |f|
  = f.error_notification

  .form-inputs
    = f.association :patient
    = f.association :doctor
    = f.input :diseases

  .form-actions
    = f.button :submit
